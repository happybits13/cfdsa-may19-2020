In these examples, using the new image for nwapp. Hence, no need define configmap anymore. Config alrdy in image.

In this eg, will be implementing:
1) ingress to direct to multiple host
2) ingress to direct to multiple path (also using regex to redirect to desired path)

3) rolling updates
